# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true

# Deterministic builds
android.injected.build.api=33
android.injected.build.abi=arm64-v8a,armeabi-v7a,x86,x86_64

# Release signing configuration (override in local.properties)
# Do NOT commit actual keystore files or passwords to version control
# See keystore/README.md for setup instructions
