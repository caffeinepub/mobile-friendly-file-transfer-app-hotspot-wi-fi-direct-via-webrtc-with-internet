# Release Signing Keystore

This directory should contain your release signing keystore file, but **NEVER commit it to version control**.

## Generating a Release Keystore

Run this command from the `frontend/android/keystore` directory:

